<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import de.jensd.fx.glyphs.fontawesome.FontAwesomeIconView?>

<VBox xmlns="http://javafx.com/javafx/11.0.1"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.controller.MainController"
      styleClass="main-container"
      spacing="0">

    <!-- Верхняя панель с градиентом -->
    <HBox style="-fx-background-color: linear-gradient(135deg, #6366F1 0%, #8B5CF6 50%, #06D6A0 100%);
                 -fx-padding: 30px 40px; -fx-spacing: 20;"
          alignment="CENTER_LEFT">

        <HBox spacing="20" alignment="CENTER_LEFT">
            <FontAwesomeIconView glyphName="FILE_PDF_ALT" styleClass="icon" size="36"
                                 style="-fx-fill: white; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5,0,2,2);"/>
            <VBox spacing="5">
                <Label text="PDF SIGNER PRO" styleClass="futuristic-title"/>
                <Label text="Профессиональная система электронной подписи" styleClass="subtitle"/>
            </VBox>
        </HBox>

        <Region HBox.hgrow="ALWAYS"/>

        <Button text="НОВЫЙ ДОКУМЕНТ" onAction="#handleNewDocument" styleClass="neomorphic-button">
            <graphic>
                <FontAwesomeIconView glyphName="FILE_ALT" styleClass="icon"/>
            </graphic>
        </Button>
    </HBox>

    <!-- Основной контент -->
    <VBox fx:id="mainContainer" style="-fx-padding: 40px; -fx-spacing: 30;"
          alignment="TOP_CENTER">

        <!-- Карточка выбора документа -->
        <VBox styleClass="glass-card" spacing="20" prefWidth="800">
            <padding>
                <Insets top="30" right="30" bottom="30" left="30"/>
            </padding>

            <Label text="📄 ВЫБОР ДОКУМЕНТА" styleClass="gradient-title"/>

            <HBox spacing="20" alignment="CENTER_LEFT">
                <VBox spacing="10">
                    <Label text="Тип документа" styleClass="subtitle"/>
                    <ComboBox fx:id="docTypeComboBox" styleClass="modern-combo" prefWidth="350"/>
                </VBox>

                <VBox spacing="10">
                    <Label text="Действия" styleClass="subtitle"/>
                    <Button text="📁 ВЫБРАТЬ ФАЙЛЫ" onAction="#handleSelectFiles"
                            styleClass="gradient-button">
                        <graphic>
                            <FontAwesomeIconView glyphName="FOLDER_OPEN" styleClass="icon"/>
                        </graphic>
                    </Button>
                </VBox>
            </HBox>
        </VBox>

        <!-- Панель инструментов -->
        <HBox styleClass="glass-toolbar" spacing="15" alignment="CENTER">
            <Button text="👤 ПОДПИСЬ СОТРУДНИКА" onAction="#handleAddEmployeeSignature"
                    styleClass="neomorphic-button"/>
            <Button text="📋 ДОВЕРЕННОСТЬ" onAction="#handleAddProxy"
                    styleClass="neomorphic-button"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="✅ ПОДПИСАТЬ" onAction="#handleSign"
                    styleClass="gradient-button success-button"/>
            <Button text="📊 ПРОТОКОЛ" onAction="#handleCreateProtocol"
                    styleClass="gradient-button warning-button"/>
        </HBox>

        <!-- Область статуса -->
        <VBox styleClass="glass-card" spacing="15" prefWidth="800">
            <padding>
                <Insets top="25" right="25" bottom="25" left="25"/>
            </padding>

            <HBox spacing="15" alignment="CENTER_LEFT">
                <FontAwesomeIconView glyphName="TASKS" styleClass="icon-primary" size="20"/>
                <Label text="СТАТУС ОПЕРАЦИЙ" styleClass="subtitle"
                       style="-fx-font-size: 18px; -fx-text-fill: white;"/>
            </HBox>

            <TextArea fx:id="statusTextArea" styleClass="glass-text-area"
                      prefRowCount="10" wrapText="true"/>
        </VBox>

    </VBox>

    <!-- Статус бар -->
    <HBox styleClass="glass-status-bar" spacing="20" alignment="CENTER_LEFT">
        <HBox spacing="10" alignment="CENTER_LEFT">
            <FontAwesomeIconView glyphName="CIRCLE" size="8"
                                 style="-fx-fill: #06D6A0; -fx-effect: dropshadow(gaussian, #06D6A0, 5,0,0,0);"/>
            <Label text="Система активна" style="-fx-text-fill: #94A3B8; -fx-font-weight: 600;"/>
        </HBox>

        <Region HBox.hgrow="ALWAYS"/>

        <HBox spacing="15" alignment="CENTER_RIGHT">
            <Label text="PDF SIGNER PRO v2.0"
                   style="-fx-text-fill: #6366F1; -fx-font-weight: 800; -fx-font-size: 12px;"/>
            <Label text="|" style="-fx-text-fill: #475569;"/>
            <Label text="READY"
                   style="-fx-text-fill: #06D6A0; -fx-font-weight: 900; -fx-font-size: 12px;"/>
        </HBox>
    </HBox>

</VBox>